<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NoteForge.Views.VaultPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NoteForge.Views"
    xmlns:models="using:NoteForge.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#262626">

    <Grid Padding="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Main Content -->
        <Grid Grid.Row="1" Padding="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Spacing="10">
                <TextBlock Text="NoteForge" 
                           FontSize="32" 
                           FontWeight="Bold" 
                           Foreground="#DADADA" />
                <TextBlock Text="Your personal knowledge base" 
                           FontSize="16" 
                           Foreground="#888888" />
            </StackPanel>

            <!-- Recent Vaults List -->
            <StackPanel Grid.Row="1" Margin="0,40,0,0" Spacing="15">
                <TextBlock Text="Recent Vaults" 
                           FontSize="14" 
                           FontWeight="Bold" 
                           Foreground="#DADADA" />
                 
                <!-- Empty State -->
                <TextBlock x:Name="EmptyStateLabel"
                           Text="No recent vaults found." 
                           Foreground="#888888" 
                           FontSize="14"
                           Opacity="0.7" 
                           Visibility="Collapsed"/>

                <ListView x:Name="RecentVaultsCollection" 
                          SelectionMode="Single"
                          SelectionChanged="OnRecentVaultSelected"
                          Background="Transparent">
                    <ListView.Resources>
                        <SolidColorBrush x:Key="ListViewItemBackgroundPointerOver" Color="#343434"/>
                        <SolidColorBrush x:Key="ListViewItemBackgroundPressed" Color="#3A3A3A"/>
                        <SolidColorBrush x:Key="ListViewItemBackgroundSelected" Color="Transparent"/>
                        <SolidColorBrush x:Key="ListViewItemBackgroundSelectedPointerOver" Color="#343434"/>
                        <SolidColorBrush x:Key="ListViewItemBackgroundSelectedPressed" Color="#3A3A3A"/>
                    </ListView.Resources>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0,0,0,5" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:VaultInfo">
                            <Border Padding="12"
                                    Background="Transparent"
                                    CornerRadius="6">
                                <StackPanel Spacing="3">
                                    <TextBlock Text="{x:Bind Name}" 
                                               Foreground="#DADADA" 
                                               FontWeight="SemiBold" 
                                               FontSize="14"/>
                                    <TextBlock Text="{x:Bind Path}" 
                                               Foreground="#888888" 
                                               FontSize="12" 
                                               TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>

            <!-- Actions Footer -->
            <StackPanel Grid.Row="2" Spacing="0">
                <Rectangle Height="1" Fill="#404040" Margin="0,0,0,20" />
                
                <Grid ColumnDefinitions="*, Auto" ColumnSpacing="20">
                    <StackPanel Grid.Column="0" Spacing="5">
                        <TextBlock Text="Open folder as vault" 
                                   Foreground="#DADADA" 
                                   FontWeight="SemiBold"/>
                         <TextBlock Text="Open an existing folder of Markdown files" 
                                    Foreground="#888888" 
                                    FontSize="12"/>
                    </StackPanel>
                    
                    <Button Grid.Column="1"
                            Content="Open"
                            Click="OnOpenVaultClicked"
                            Background="#343434"
                            Foreground="#DADADA"
                            Width="100"
                            Height="36"
                            CornerRadius="6"/>
                </Grid>

                <Rectangle Height="1" Fill="#404040" Margin="0,15" />

                <Grid ColumnDefinitions="*, Auto" ColumnSpacing="20">
                    <StackPanel Grid.Column="0" Spacing="5">
                        <TextBlock Text="Create new vault" 
                                   Foreground="#DADADA" 
                                   FontWeight="SemiBold"/>
                         <TextBlock Text="Create a new NoteForge vault under a folder" 
                                    Foreground="#888888" 
                                    FontSize="12"/>
                    </StackPanel>
                    
                    <Button Grid.Column="1"
                            Content="Create"
                            Click="OnCreateVaultClicked"
                            Background="#343434"
                            Foreground="#DADADA"
                            Width="100"
                            Height="36"
                            CornerRadius="6"/>
                </Grid>
            </StackPanel>
            
        </Grid>
    </Grid>
</Page>

